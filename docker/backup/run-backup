#!/usr/bin/env bash
# Cron-side wrapper: load /etc/hc-map/backup.env (rendered by entrypoint.sh)
# and exec backup.sh with the given period.
set -euo pipefail
# shellcheck source=/dev/null
. /etc/hc-map/backup.env
exec /usr/local/bin/backup.sh "$@"
